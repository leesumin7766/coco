global:
  scrape_interval: 5s

scrape_configs:
  # Spring Boot
  - job_name: 'coco-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['backend:8080']

  # Redis Exporter (캐시 히트율)
  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']
